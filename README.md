# app2іащіщацда

## Backend

The backend is a NestJS application providing REST API endpoints for the SEAP (Smart Livestock Management Platform).

### Setup
```bash
cd backend
npm install
cp .env.example .env
# Configure your database settings in .env
npm run migration:run
npm run start:dev
```

See `backend/README.md` for detailed backend documentation.
